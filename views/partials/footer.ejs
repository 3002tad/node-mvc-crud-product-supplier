    </main>
    
    <!-- Footer -->
    <footer class="bg-dark text-light py-5 mt-auto">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4">
                    <h5 class="mb-3">
                        <i class="fas fa-boxes text-primary"></i> 
                        Product Supplier Management
                    </h5>
                    <p class="text-light-emphasis">
                        Hệ thống quản lý sản phẩm và nhà cung cấp hiện đại, 
                        được xây dựng với công nghệ Node.js và MongoDB.
                    </p>
                    <div class="d-flex gap-3">
                        <a href="/suppliers" class="text-decoration-none text-light-emphasis">
                            <i class="fab fa-github"></i>
                        </a>
                        <a href="/products" class="text-decoration-none text-light-emphasis">
                            <i class="fas fa-globe"></i>
                        </a>
                    </div>
                </div>
                
                <div class="col-lg-2 col-md-6">
                    <h6 class="mb-3">Nhà cung cấp</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <a href="/suppliers" class="text-decoration-none text-light-emphasis">
                                <i class="fas fa-list me-1"></i> Danh sách
                            </a>
                        </li>
                        <li class="mb-2">
                            <a href="/suppliers/new" class="text-decoration-none text-light-emphasis">
                                <i class="fas fa-plus me-1"></i> Thêm mới
                            </a>
                        </li>
                    </ul>
                </div>
                
                <div class="col-lg-2 col-md-6">
                    <h6 class="mb-3">Sản phẩm</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2">
                            <a href="/products" class="text-decoration-none text-light-emphasis">
                                <i class="fas fa-list me-1"></i> Danh sách
                            </a>
                        </li>
                        <li class="mb-2">
                            <a href="/products/new" class="text-decoration-none text-light-emphasis">
                                <i class="fas fa-plus me-1"></i> Thêm mới
                            </a>
                        </li>
                    </ul>
                </div>
                
                <div class="col-lg-4">
                    <h6 class="mb-3">Công nghệ</h6>
                    <div class="row g-2">
                        <div class="col-6">
                            <span class="badge bg-primary me-1">Node.js</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-success me-1">MongoDB</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-info me-1">Express</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-warning me-1">Bootstrap 5</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-danger me-1">EJS</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-secondary me-1">Mongoose</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <hr class="my-4 text-light-emphasis">
            
            <div class="row align-items-center">
                <div class="col-md-6">
                    <p class="mb-0 text-light-emphasis">
                        &copy; 2025 Product Supplier Management System
                    </p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0 text-light-emphasis">
                        Built with <i class="fas fa-heart text-danger"></i> using MVC Architecture
                    </p>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Bootstrap 5 JavaScript Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JavaScript -->
    <script>
        // Enhanced confirmation for delete actions
        function confirmDelete(item) {
            return confirm(`Bạn có chắc chắn muốn xóa ${item} này không? Hành động này không thể hoàn tác.`);
        }
        
        // Auto-hide alerts with animation
        document.addEventListener('DOMContentLoaded', function() {
            const alerts = document.querySelectorAll('.alert:not(.alert-permanent)');
            alerts.forEach(alert => {
                // Add fade-in animation
                alert.classList.add('fade-in');
                
                // Auto-hide after 5 seconds
                setTimeout(() => {
                    alert.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                    alert.style.opacity = '0';
                    alert.style.transform = 'translateY(-20px)';
                    setTimeout(() => alert.remove(), 500);
                }, 5000);
            });
            
            // Enhanced form submission handling
            const forms = document.querySelectorAll('form');
            forms.forEach(form => {
                form.addEventListener('submit', function(e) {
                    const submitBtn = this.querySelector('button[type="submit"]');
                    const deleteBtn = this.querySelector('button[title*="Xóa"], button[title*="Delete"]');
                    
                    // Handle delete confirmation
                    if (deleteBtn && e.target === deleteBtn) {
                        const item = this.action.includes('suppliers') ? 'nhà cung cấp' : 'sản phẩm';
                        if (!confirmDelete(item)) {
                            e.preventDefault();
                            return false;
                        }
                    }
                    
                    // Handle form validation
                    if (this.classList.contains('needs-validation')) {
                        if (!this.checkValidity()) {
                            e.preventDefault();
                            e.stopPropagation();
                            this.classList.add('was-validated');
                            return false;
                        }
                    }
                    
                    // Show loading state for submit buttons
                    if (submitBtn) {
                        const originalText = submitBtn.innerHTML;
                        submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Đang xử lý...';
                        submitBtn.disabled = true;
                        
                        // Reset button state after timeout (fallback)
                        setTimeout(() => {
                            submitBtn.innerHTML = originalText;
                            submitBtn.disabled = false;
                        }, 10000);
                    }
                });
            });
            
            // Add hover effects to cards
            const cards = document.querySelectorAll('.card');
            cards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-5px)';
                });
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
        });
        
        // Toast notifications
        function showToast(message, type = 'success') {
            const toastContainer = document.querySelector('.toast-container') || createToastContainer();
            const toast = document.createElement('div');
            toast.className = `toast align-items-center text-white bg-${type} border-0`;
            toast.setAttribute('role', 'alert');
            toast.innerHTML = `
                <div class="d-flex">
                    <div class="toast-body">${message}</div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                </div>
            `;
            toastContainer.appendChild(toast);
            const bsToast = new bootstrap.Toast(toast);
            bsToast.show();
            
            toast.addEventListener('hidden.bs.toast', () => toast.remove());
        }
        
        function createToastContainer() {
            const container = document.createElement('div');
            container.className = 'toast-container position-fixed bottom-0 end-0 p-3';
            document.body.appendChild(container);
            return container;
        }
        
        // Debug: Log form submissions
        window.addEventListener('beforeunload', function() {
            console.log('Page is being unloaded');
        });
    </script>
</body>
</html>